{% set name = "python-magic" %}
{% set version = "0.4.22" %}
{% set hash_val = "ca884349f2c92ce830e3f498c5b7c7051fe2942c3ee4332f65213b8ebff15a62" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ hash_val }}
  patches:
    # - discover-libmagic-dylib.patch  # [osx]

build:
  number: 1
  script: {{ PYTHON }} -m pip install . --no-deps -vv
  skip: true  # [win]

requirements:
  host:
    - pip
    - python
  run:
    - libmagic
    - python

test:
  imports:
    - magic

about:
  home: http://github.com/ahupp/python-magic
  license: MIT
  license_file: LICENSE
  license_family: MIT
  summary: File type identification using libmagic

extra:
  recipe-maintainers:
    - mariusvniekerk
